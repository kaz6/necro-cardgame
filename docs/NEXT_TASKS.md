# NEXT_TASKS

次の作業単位。上から順に着手する。
進捗の全体像は `SESSION_STATE.md` を参照。

---

## 0. 人間の判断待ち（着手前に解決が必要）

- [x] **召喚元の墓地はどちらか** → 2026-08-11 訂正で「自分の墓地」に確定。実装反映済み
- [x] **能力持ち4種の効果テキスト** → 2026-08-11（CG-005）で確定。実装・計測とも完了
- [x] **呪いの効果の再設計** → 2026-08-11（CG-005）で
      「相手の墓地からの召喚の最初の1回に +1pt」に確定
- [x] **盤面が厚くならなかったのは AI の性質か** → 2026-08-11（CG-006）の裁定 A で
      「評価関数ベースの CPU を作って測り直す」と決まり、実施済み。**AI 由来ではなかった**
- [x] **★ トークンの 0pt 再召喚に歯止めを入れるか** → 2026-08-11（CG-007）の裁定 A で
      「倒されたトークンは相手の墓地へ送らず消滅する」に確定。実装・計測とも完了
      （`tokens.list[].onDeath = 'vanish'`。未決着 150 → 5 戦・埋まり率 67.4% → 28.3%）
- [ ] **★ 盤面の厚みで仕様を触るか。** 抜け道を除いても 28.3% どまりで、
      ターン6以降は約2.0/6枠に収束。A（ピッチの交換レートを緩める）を推奨。
      ★ **人間がプレイしてから判断すると決まった（CG-007）。それまで触らない**
- [ ] **★「墓地からの召喚＝奪ったものを使役する」の表現をどうするか。**
      里帰り（自分の持ち札が自分の墓地へ戻る）がある。A（表現を直す）を推奨。
      同上（人間のプレイ待ち）

---

## 1. カード能力の実装

- [x] `ability` のスキーマを確定する（`trigger` / `effect` / `params` / `text`）
- [x] 死後発動効果の解決タイミングを engine に入れる（`resolveDeath` / `resolveKillTrigger`）
- [x] 呪いの実装（`graveyardSummonTax`）
- [x] `c05`〜`c08` の `implemented` を true にする
- [x] テキストに書かれていない解釈を `rules.abilities` でフラグ化する

制約: 数値・効果はすべて JSON に置く。JS にベタ書きしない。

---

## 2. 計測

- [x] **自動プレイの基礎統計**（CG-001・2026-08-11）
      → `docs/research/自動プレイ基礎統計_20260811.md`。ランダムAI 1000戦の観測値
      ※ ランダムAIの結果は人間のプレイとは別物。構造的な偏りを見るためのもの
- [x] **能力実装後に測り直す**（CG-005・2026-08-11・同じシード帯 1000〜1999）
      → `docs/research/自動プレイ基礎統計_20260811_CG005.md`。前後比較つき
- [x] **CPU にすると埋まり率が上がるか**（CG-006・2026-08-11・同じシード帯 1000〜1999）
      → `docs/research/CPU対戦比較_20260811_CG006.md`。ランダム／CPU の3列
      ※ AI は `tools/harness.js` で差し替える。ランダム側の乱数の消費順は CG-001 から不変
- [x] **トークンを消滅させた前後**（CG-007・2026-08-11・同じシード帯 1000〜1999）
      → `docs/research/トークン消滅_前後比較_20260811_CG007.md`。前／前・自粛／後の3列
      ※ `tokens.list[].onDeath` を読み込み時に差し替えて測る。JSON は書き換えない

再現:

```
node tools/simulate.js 1000 1000 random
node tools/simulate.js 1000 1000 cpu
SIM_REVIVE_TOKENS=0 node tools/simulate.js 1000 1000 cpu

SIM_GAMES=1000 SIM_SEED=1000 SIM_OUT="自動プレイ基礎統計_20260811_CG005.md" \
  SIM_TITLE="CG-005 能力実装後" node tools/report.js "$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M JST')" "$(git rev-parse --short HEAD)"

SIM_GAMES=1000 SIM_SEED=1000 SIM_OUT="CPU対戦比較_20260811_CG006.md" \
  node tools/compare.js "$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M JST')" "$(git rev-parse --short HEAD)"

SIM_GAMES=1000 SIM_SEED=1000 SIM_OUT="トークン消滅_前後比較_20260811_CG007.md" \
  node tools/compare_tokens.js "$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M JST')" "$(git rev-parse --short HEAD)"
```

★ `tools/compare.js`（CG-006）は**トークンが墓地へ行く前提の3列**なので、
いま回すと CG-007 後のルールで測った別物になる。前後比較には `tools/compare_tokens.js` を使う。

---

## 2.5 動作環境（CG-008・完了）

- [x] **file:// で開けるようにする**（2026-08-11・CG-008）
      ES モジュールと `fetch` をやめ、素の `<script>` ＋ 即時関数に組み替えた。
      `index.html` をダブルクリックするだけで遊べる（サーバ不要・ビルド工程なし）。
      データは `data/*.js`（中身は素の JSON）に一本化。
      → 判断の記録は `DECISION_LOG.md`「file:// から開けるようにする（CG-008）」
- [x] 致命的エラーを画面に出す（`index.html` の `#fatal`。真っ黒のままにしない）
- [ ] ★ **Windows Chrome での最終確認**（実装側の検証は Chromium/Linux の file:// で実施済み。
      作者の実機での確認だけが残っている）

---

## 2.7 デバッグ支援（CG-013・完了）

- [x] ルール切り替えパネル（index.html の畳めるデバッグパネル。反映は次の新規対戦から）
      墓地バッファ 0/1/2 ／ ドロー 2/3 ／ 後攻初手 6/7 ／ トークンの倒れ先 消滅/墓地へ
- [x] 対戦ログの書き出し（設定値・シード・全行動・決着ターン・勝敗。コピー/保存とも file:// で動作）
- [x] 解析ツール `tools/analyze_logs.js`（複数ログを設定値グループ別に集計。
      ピッチ専用率のターン別推移・コスト別の出/捨比・墓地に欲しいカードが無い頻度・
      呪いの関与・攻撃力/体力の偏り）

---

## 2.8 能力アイコンとピッチ持ち越し（CG-015・完了）

- [x] 能力アイコンの常時表示（◆場/◆撃/◆倒/◆墓。手札・盤面・墓地すべてで見える。
      CG-013 まではツールチップのみで見えていなかった）
- [x] ピッチptのターン内持ち越しを `rules.pitchCarryover` でフラグ化（既定 false＝従来。
      デバッグパネルから切り替え。残ptはステータス行と召喚UIに常時表示。ターン終了で失効）
- [x] あり/なしの比較 500戦（シード 1000〜1499）
      → `docs/research/ピッチ持ち越し_前後比較_20260814_CG015.md`
- [ ] 採否の判断（人間のプレイテスト後。`SESSION_STATE.md` の未確定項目）

---

## 3. プレイテスト

- [ ] 人間2人でホットシート対戦を回す（`index.html` の対戦相手＝人間 vs 人間）
- [ ] ★ **対戦ごとにログを保存する（デバッグパネル）。** 墓地バッファ 0/2 を切り替えて遊び、
      `node tools/analyze_logs.js <ログ...>` で並べて比べる（CG-013 の目的）
- [ ] 人間 vs CPU も回す（CPU の手が理不尽・退屈でないかを見る。強さの評価ではない）
- [ ] 検証したい仮説を測る: **倒されたカードが相手に渡ることが、ジレンマとして体感できるか**
- [ ] 墓地の総コストの推移を観測する（何を紐付けるかはその後に決める）
- [ ] 小型量産が支配的にならないか観測する（盤面6枠だけを歯止めとして素で測る）
- [ ] 先攻・後攻のバランス（初手6/7 と 先攻1ターン目ドローの是非）

---

## 4. 未確定ルールの確定

`SESSION_STATE.md` の未確定項目のうち、プレイテストで判断できるもの。

- [ ] 配置換えのコスト（現状0）
- [ ] 召喚酔いの有無（現状なし）
- [ ] ネクロマンサー本体への攻撃条件（現状: 相手の場が空のときのみ）
- [ ] デッキ枚数（現状30）
- [ ] ネクロマンサーが倒された後、使役対象になるか

---

## 5. オンライン対戦（先の話）

engine 側の前提はすでに満たしてある（純粋関数・決定的・`filterStateFor`）。
CPU も同じ前提で書いてあるので、サーバ側でそのまま動く（`js/ai.js`）。

- [ ] 権威サーバの実装。クライアントは action のみ送信
- [ ] サーバは `filterStateFor` を通した state だけを各クライアントへ返す
- [ ] 認証（ランクマッチ・クロスセーブをやるなら必須）

---

## 6. 段階2以降（検証完了後）

- [ ] カードプール→デッキ作成の工程（`REJECTED.md`「保留（種）」）
      ドラフトをここで復活させる案あり
