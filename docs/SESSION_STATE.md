# SESSION_STATE

> **① 転記日時: 2026-08-11 11:37 JST（転記コミット `8d65015`）／この宣言の追記: 2026-08-11 12:30 JST**
> **② これは写しであって正本ではない。正本は Notion の「SESSION_STATE」ページ（`3b8a8a5d-fd45-81de-aeb2-f19d5c5275d1`）。**
> 食い違ったら Notion が正しい。この写しを根拠に仕様を判断しない。
> **③ このファイルを直接編集しても Notion には戻らない。** 正本を変えるときは Notion 側を先に直し、そのうえでここへ転記し直すこと。

作業再開用のスナップショット。**セッションの最初にここを読むこと。**

最終更新: 2026-08-11 21:40 JST ／ HEAD: 末尾の commit を参照

---

## 現在の進捗

**ルール v0.2 の骨格＋カード能力4種を実装済み。ローカル2人ホットシートで完走できる状態。**
**2026-08-11 の訂正（召喚元＝自分の墓地）を反映済み。**
**CG-005 で c05〜c08 の効果を実装し、同一条件で前後比較の計測まで完了。**

動くもの:

- `js/engine.js` — v0.2 のルール一式（純粋関数・seeded RNG・`filterStateFor`）
- `js/view.js` + `index.html` — ホットシート UI。目隠しカーテン付き
- `data/cards.json` — カード8種 + ネクロマンサー + ルールフラグ + デッキ構成

実装済みのルール:

- ゾーン（デッキ / 手札 / 場3×2 / 墓地 / ネクロマンサー専用スロット）
- 一貫法則（自分の手を離れたカードは必ず相手の墓地へ）
- ピッチコスト制の召喚（捨てたコスト合計の範囲で、**手札または自分の墓地から** 1:1 変換）
- 前列が残る間、同列の後列は攻撃されない
- 戦闘（相打ちあり・体力は回復しない）／倒したカードは倒した側の墓地へ
- 毎ターン2枚ドロー・デッキ切れ敗北なし・初手 先攻6/後攻7
- 配置換え（現状コスト0）
- ネクロマンサー撃破で勝利
- 墓地の総コスト常時表示（表示のみ。ゲーム効果なし）
- **カード能力4種（CG-005）:**
  - `c05` 倒されたとき 1/1 になって後列へ退く（後列が埋まっていれば不発 → 相手の墓地へ）
  - `c06` 場に出たとき、右の空き枠に 1/1 のトークン（右端／右が埋まりなら何も起きない）
  - `c07` 相手のカードを倒したとき、相手のデッキ上1枚を自分の墓地へ（反撃で倒したときも発動）
  - `c08` 呪い。相手の墓地にある間、相手の墓地召喚の最初の1回に +1pt（複数枚は加算・同時消費）
- **能力の解釈フラグ:** `data/cards.json` の `rules.abilities` に4つ。engine にハードコードなし

**計測:** `tools/simulate.js` でランダムAI同士の自動対戦を回せる（view 非依存・Node 単体）。

- 能力実装前 1000戦: `docs/research/自動プレイ基礎統計_20260811.md`（HEAD `88a89af`）
- 能力実装後 1000戦: `docs/research/自動プレイ基礎統計_20260811_CG005.md`（HEAD `ec2bd24`・前後比較つき）

**未実装:** なし（カード8種すべて実装済み）。

---

## 次にやること

1. **★ 盤面が厚くならなかった件の裁定を受ける。** 能力4種を入れても
   盤面の平均埋まり率は 26.0% → 26.8%（+0.8pt）にしか動かず、
   **前後列の判断が発生する水準には届いていない。** 対処するかどうかの判断待ち
   （詳細は「判断待ち」節）
2. 実際に人間2人でホットシート対戦を回し、
   検証したい仮説（奪われることがジレンマとして体感できるか）を測る
3. 1 の裁定が出たら、その方向で `data/cards.json` を調整して同じシード帯で測り直す

詳細は `NEXT_TASKS.md`。

---

## 運用（2026-08-11 に確立）

- **タスクは Notion 経由。** `_tasks（実装タスク）` 配下の `進行中` / `完了済み`。接頭辞は `CG-`
- **次に使う番号: CG-006**
- 実装結果はタスクページの「実装結果」節に書き、チャットには短い報告のみ返す
- セッション終了は `.claude/skills/session-end/SKILL.md` に従う
- **本企画は SESSION_STATE・CURRENT_SPEC とも Notion が正本。** 全置換せず外科的更新のみ
- ★ **向きは文書ごとに独立。** SESSION_STATE は Notion 固定、CURRENT_SPEC のみ将来 repo へ反転する

---

## 未確定項目

**勝手に確定させないこと。** ここにある項目は人間の判断待ち。

- **★ 盤面が厚くならない件をどう扱うか。** 能力4種を入れても平均埋まり率は 26.8% どまり。
  **前後列の判断が発生していない。** 対処するか、人間のプレイテストまで保留するかが未決（下記「判断待ち」）
- **c06 のトークンがコスト0で再召喚できる件。** 相手の墓地に落ちたトークンは 0pt で出し直せる。
  「トークンのコストは0」の直接の帰結として意図的に残してある。歯止めを入れるかは未決
- **ネクロマンサー本体への攻撃条件**。仮ルール「相手の場に1体でもいれば本体を攻撃できない」で実装済み
  （`rules.necromancerProtectedWhileBoardOccupied`）
- **ネクロマンサーが倒された後、使役対象になるか**。現状は撃破＝即決着で、その先を実装していない
- **配置換えのコスト**。現状0（`rules.repositionCost`）。回数制限も無制限（`rules.repositionPerTurn`）
- **デッキ枚数**。現状は仮の30枚（`deck.size`）
- **小型量産が支配的にならないか**。盤面6枠のみを歯止めとして素で測る段階
- **ネクロマンサーの正体**（魔女化した図書館司書）。図書館パニック側が未確定のため保留
- **召喚酔いの有無**。正本に記述がないため**なし**で実装（`rules.summoningSickness: false`）。
  現状、召喚したターンにそのまま攻撃できる。
  ★ CG-005 の計測で、**先攻1ターン目に決着した試合が1件出た**（シード1358）。
  召喚酔いなし＋盤面が空なら本体を狙える、の組み合わせによる
- **先攻1ターン目のドロー**。正本に記述がないため**あり**で実装（`rules.firstPlayerDrawsOnTurn1: true`）。
  先攻6/後攻7 の差と合わせて先攻有利かどうかは未検証

---

## 判断待ち

> ★ **判断が済んだら、次のセッションでこの節を消すこと。**

### 要裁定: CG-005（能力持ち4種の実装）— 盤面が厚くならなかった

**日時:** 2026-08-11 21:40 JST
**タスクページ:** `_tasks（実装タスク）` 配下の CG-005（親: `3b8a8a5d-fd45-8197-9f70-d0e222660c63`）
**書いた節:** 実装結果

**論点:** 能力4種を入れても盤面の平均埋まり率が 26.0% → 26.8%（+0.8pt）にしか動かず、
前後列の判断が発生する水準に届いていない。これに対処するか。

**A（推奨）: 何もせず、まず人間2人のプレイテストへ進む。**
ランダムAIは盤面を維持する動機を持たず、攻撃できる限り毎ターン殴って盤面を溶かす。
埋まり率が上がらないのは能力の弱さではなく AI の性質かもしれず、
いま数値を触ると「AI 向けの調整」を仕様に焼き込むことになる。

**B: 先に盤面が増える方向へ調整する**（ピッチの交換レート・c06 のトークン数・盤面枠数のいずれか）。

**トレードオフ:** A＝人間のデータが出るまで判断を遅らせるが、誤った調整を焼き込まない。
B＝すぐ厚くできるが、ランダムAIの癖に最適化した仕様が残る危険がある。

**却下済み:** なし（この論点に該当する却下案はない）。

---

## 解決済み（履歴）

- ~~能力持ち4種の効果テキスト~~ → **2026-08-11（CG-005）で確定。実装・計測とも完了**
- ~~呪いの効果の再設計~~ → **2026-08-11（CG-005）で「墓地からの召喚の最初の1回に +1pt」に確定。
  常在型は却下済み（`REJECTED.md`）**
- ~~召喚元の墓地はどちらか~~ → **2026-08-11 訂正で「自分の墓地」に確定。実装反映済み**
- ~~世界観の時系列の断定~~ → **2026-08-11 訂正で「if 世界の枝」に表記を緩めた。docs 反映済み**
