# SESSION_STATE

作業再開用のスナップショット。**セッションの最初にここを読むこと。**

最終更新: 2026-08-11

---

## 現在の進捗

**ルール骨格 確定。実装ゼロ。**

- ルールの骨格は `DECISION_LOG.md` に確定事項として記録済み。
- 却下案は `REJECTED.md` に理由込みで記録済み。
- リポジトリは雛形のみ。`js/engine.js` / `js/view.js` / `data/cards.json` は
  いずれも中身が入っていない。

---

## 次にやること

1. **`data/cards.json` の8種定義**
   カード8種（うち能力持ち4種 = 呪い1種 + 有効効果3種）を JSON で定義する。
   数値・効果はすべて JSON 側に置く。JS にベタ書きしない。

2. **`js/engine.js` の1ターン処理**
   `(state, action) => newState` の純粋関数として、1ターン分の処理を実装する。
   同時に `filterStateFor(state, playerId)` と seeded RNG を用意する。

詳細は `NEXT_TASKS.md`。

---

## 未確定項目

**勝手に確定させないこと。** ここにある項目は人間の判断待ち。

- **能力持ち3種の具体的効果テキスト**
  有効効果を持つ3種が何をするか、まだ決まっていない。

- **マナカーブ**
  マナは「あり・固定増加」までは確定。増加量とカードのコスト分布は未定。

- **呪いのノイズ判定**
  呪い2枚がデッキ30枚の中でどれだけ事故要因になるか未検証。
  引きたくないカードとして機能しすぎないか、実際に回して確認が必要。
