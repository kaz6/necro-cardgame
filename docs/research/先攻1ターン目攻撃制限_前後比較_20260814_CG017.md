# 先攻1ターン目の攻撃制限：前後比較（CG-017）

調査日: 2026-08-14

## TL;DR

`data/cards.js` の `rules.firstPlayerAttacksOnTurn1` だけを差し替えて、同じシード帯・同じ AI で
500 戦ずつ回した。デッキ・AI の重み・他のルールは一切変えていない。
**本ファイルは観測値のみを記載する。解釈と結論は書かない。**

> ★ **自動対戦の結果は人間のプレイとは別物である。**
> これは「構造的に偏るか」を見るためのものであって、面白いかどうかの判定には使わない。
> 本フラグは CPU では差が出ない可能性を承知のうえで、作者が人間プレイで
> 比較するために用意した（CG-017 の指示）。

---

## 測定の条件

| 項目 | 値 |
|---|---|
| 実施日時 | 2026-08-14 09:42 JST |
| HEAD | `6228e10` |
| シード | 1000〜1499（1試合ごとに +1） |
| 試行回数 | 500 戦（列ごとに同じシード帯） |
| ターン上限 | 300（打ち切り時は未決着として計上） |
| 制限なし | `rules.firstPlayerAttacksOnTurn1 = true`（従来の挙動。`data/cards.js` の既定） |
| 制限あり | `rules.firstPlayerAttacksOnTurn1 = false`（先攻の最初の手番は攻撃不可。召喚・配置換えは可能） |
| AI | 評価関数CPU（js/ai.js。filterStateFor 済みの state だけを見て1手先を評価する貪欲法）。重みは `data/ai.js`（両列とも同一） |
| デッキ | `deck.lists.default` 30枚・両者同一 |
| 乱数 | engine の seeded RNG のみ。CPU は乱数を使わない（同一 state → 同一手） |

## 定義（この表の数え方）

- **1ターン目**: 先攻プレイヤーの最初の手番（`state.turn === 1`）
- **1ターン目終了時点の後攻ネクロマンサーの残り体力**:
  turn が 1 → 2 に変わった瞬間（＝先攻の最初の手番の endTurn 直後）の
  後攻ネクロマンサーの（体力 − 受けたダメージ）。初期体力は 12。
  1ターン目中に決着した試合はその時点の値（下限0）で数える。全試合が母数

---

## 1. 指示された3項目

| 指標 | 制限なし（従来） | 制限あり |
|---|---|---|
| 先攻勝率 | 50.2% | 19.1% |
| 平均決着ターン数 | 7.7 | 11.7 |
| ★ 1ターン目終了時点の後攻ネクロマンサーの残り体力（平均） | 2.16（最小 0・母数 500 戦） | 12.00（最小 12・母数 500 戦） |

---

## 2. その他の指標

| 指標 | 制限なし（従来） | 制限あり |
|---|---|---|
| 決着 / 未決着 | 496 / 4 | 488 / 12 |
| 決着ターン 中央値 | 7 | 10 |
| 決着ターン 範囲 | 1〜26 | 4〜37 |
| 最短試合のシード | 1002 | 1005 |
| 盤面の平均埋まり率 | 28.4%（1.70 / 6枠） | 22.9%（1.37 / 6枠） |
| 召喚元 墓地の割合 | 67.7% | 73.9% |
| 奪取（回/試合） | 16.25 | 23.01 |
| 総手数（action 数） | 34646 | 50750 |

---

## 3. 決着ターンの分布（5ターン刻み・決着した試合のみ）

| ターン | 制限なし（従来） | 制限あり |
|---|---|---|
| 1〜5 | 183 | 57 |
| 6〜10 | 189 | 195 |
| 11〜15 | 68 | 90 |
| 16〜20 | 51 | 106 |
| 21〜25 | 4 | 27 |
| 26〜30 | 1 | 12 |
| 36〜40 | 0 | 1 |

---

## 4. 能力の発動回数

500 戦の合計。engine にカウンタは持たせず、`tools/harness.js` が state の差分から数えている。

| 能力 | 制限なし（従来） | 制限あり |
|---|---|---|
| c05 後列へ退いた / 不発 | 115 / 19 | 145 / 22 |
| c06 トークン生成 / 不発 | 4302 / 52 | 6188 / 57 |
| c07 デッキ送り | 1256 | 1556 |
| c08 呪い発動（合計pt） | 1038（+1152pt） | 1524（+1695pt） |

---

## 5. 召喚されたカードのコスト分布

召喚に占める割合。コスト0はデッキに存在しない（c06 が生成するトークン）。

| コスト | 制限なし（従来） | 制限あり | デッキ構成比 |
|---|---|---|---|
| 1 | 6.3% | 6.3% | 16.7% |
| 2 | 13.2% | 13.0% | 30.0% |
| 3 | 50.6% | 52.8% | 26.7% |
| 4 | 19.2% | 17.4% | 20.0% |
| 5 | 10.7% | 10.6% | 6.7% |

---

## 再現手順

```
SIM_GAMES=500 SIM_SEED=1000 SIM_OUT="先攻1ターン目攻撃制限_前後比較_20260814_CG017.md" \
  node tools/compare_firstturn.js "$(TZ=Asia/Tokyo date '+%Y-%m-%d %H:%M JST')" "$(git rev-parse --short HEAD)"
```

「制限あり」を単体で回すときはデバッグパネル（index.html）か、
`data/cards.js` の `rules.firstPlayerAttacksOnTurn1` を `false` にする
（本スクリプトは JSON を書き換えず、読み込んだ値を差し替えて測っている）。
